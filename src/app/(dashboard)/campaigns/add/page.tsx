'use client';

import { CampaignForm } from '@/components/campaigns/campaign-form';

/**
 * Add campaign page component
 */
export default function AddCampaignPage() {
  return <CampaignForm mode="add" />;
} 